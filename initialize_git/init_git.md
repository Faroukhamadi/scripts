# Initialize git repo

**Bash**

```bash
$ python3 /path/to/init_git.py $(pwd)
```

**Fish**

```bash
$ python3 /path/to/init_git.py (pwd)
```

**Zsh**

```bash
$ python3 /path/to/init_git.py $PWD
```
