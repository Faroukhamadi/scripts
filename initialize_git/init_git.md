# Initialize git repo

**Bash**

```bash
$ python3 /path/to/init_git.py [remote_repo_name] $(pwd)
```

**Fish**

```bash
$ python3 /path/to/init_git.py [remote_repo_name] (pwd)
```

**Zsh**

```bash
$ python3 /path/to/init_git.py [remote_repo_name] $PWD
```
